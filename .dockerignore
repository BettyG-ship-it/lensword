# Virtual environments
.venv/
venv/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Git
.git/
.gitignore

# Notebooks — not needed in container
notebooks/
*.ipynb
*.ipynb_checkpoints

# Reports and screenshots — not needed in container
reports/
screenshots/

# Large tensor files — not needed in container
data/*.pt

# Keep: data/word2idx.pkl, data/*.csv — needed by API

# Stale model checkpoints — keep only lensword_model.pt
models/*.png
models/*.csv
models/lensword_model_*.pt

# Development files
.env
*.log